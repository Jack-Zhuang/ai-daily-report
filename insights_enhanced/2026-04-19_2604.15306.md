# 大语言模型的问题求解泛化能力探究：以最短路径为例

**通过最短路径规划构建可控实验环境，首次系统揭示LLM在空间迁移和长度扩展两个维度上的泛化差异及深层原因**


> 📅 预计阅读：15分钟 | 
难度：进阶 | 
arXiv: [2604.15306](http://arxiv.org/abs/2604.15306)


🏷️ 标签：`大语言模型` | `系统化泛化` | `最短路径` | `组合泛化` | `递归稳定性`


---

### 📌 TL;DR

- **一句话总结**：LLM在最短路径任务中能泛化到新地图，却无法泛化到更长路径，根源在于递归组合的不稳定性。
- **核心贡献**：构建最短路径合成环境，首次从数据覆盖、训练范式、推理策略三个维度系统分析LLM的组合泛化能力边界。
- **实用价值**：为设计更可靠的问题求解AI系统提供了理论依据，明确指出数据覆盖范围是能力上限、RL仅提升稳定性不拓展边界等关键洞察。


---

## 📖 背景与动机

大语言模型（LLM）在众多任务上展现出惊人能力，但关于其是否具备「系统化泛化」——即从已知组件组合出新解决方案的能力——学界仍存在激烈争论。现有研究面临的根本困难在于：真实场景中训练数据、模型架构、推理策略等因素相互交织，难以分离各自贡献，导致失败原因难以诊断。

本研究的核心动机是构建一个「干净」的实验环境：将最短路径规划问题作为测试床，这是一个经典的组合优化问题——给定一张图和起点终点，模型需要输出最短路径。这一选择有三重考量：最优路径可精确验证、成功标准清晰、以及「长度」这一维度天然对应组合的深度，便于考察模型在「更长的组合」上的表现。通过对比「空间迁移」（到全新地图）和「长度扩展」（到更长路径）两个正交维度，研究者试图厘清泛化失败的真实根源。


**关键要点：**

- 现有研究难以分离训练数据、模型架构、推理策略对泛化性能的影响
- 最短路径问题是理想的组合泛化测试床：最优解可精确计算、成功标准明确
- 两个正交泛化维度：空间迁移（到未见地图）和长度扩展（到更长路径）


---

## 💡 核心方法

### 方法概述

构建基于最短路径的合成实验环境，通过控制图的大小（节点数）、路径长度（边数）等参数，系统研究LLM在不同训练数据配比、不同训练范式（监督微调 vs 强化学习）、不同推理策略下的泛化表现。


### 详细设计

实验环境包含以下核心设计：

1. **图生成与任务定义**：按配置生成随机连通图，每条边权重固定为1（此时最短路径 = 最小边数路径）。输入格式为「节点列表 + 边列表 + 查询（起点→终点）」，要求模型输出具体路径。

2. **泛化维度的操作化**：
   - **空间迁移**：训练时使用N个节点的图，测试时用M个新节点的图（通常M > N）
   - **长度扩展**：训练数据中路径最长为L，测试时要求解长度为L'的路径（L' > L）

3. **训练数据配置**：采用因子化设计，系统变化数据覆盖范围——即训练集中「(起点, 终点, 路径长度)」三元组出现的比例。高覆盖意味着训练分布更密集地采样了各种长度和节点组合。

4. **训练范式对比**：
   - 监督微调（SFT）：直接让模型学习「问题→答案」的映射
   - 强化学习（PPO/DPO）：基于验证器奖励优化，验证器直接检查输出路径是否正确

5. **推理策略**：测试时采用推理时计算扩展（树搜索 + 过程奖励模型），评估模型在推理阶段「思考更久」是否能突破训练时的能力上限。

关键实验设置：图规模从5节点逐步扩展到20+节点，路径长度从3扩展到10+，覆盖比例从10%到100%梯度变化。


### 📊 方法流程图

```mermaid
```mermaid
%%{init: {'theme': 'base', 'themeVariables': {'fontSize': '16px'}}}%%
graph TD
    %% 定义样式
    classDef module fill:#e1f5fe,stroke:#01579b,stroke-width:2px
    classDef process
```

### 🔧 关键组件

| 组件 | 说明 |
|------|------|
| 最短路径验证器 | 给定图结构和起点终点，使用BFS/Dijkstra算法精确计算最短路径，用于评判模型输出的正确性（零样本验证）。这是构建「可验证」环境的基石。 |
| 覆盖率控制系统 | 通过因子化采样确保训练集中覆盖特定比例的(起点,终点,路径长度)组合，支持从10%到100%的精细调控，用于探究「见过多少例子」对泛化的影响。 |
| 过程奖励模型(PRM) | 训练一个判别器评估中间推理步骤的质量，在推理时配合树搜索策略选择更有前景的继续方向，用于测试「推理时计算」能否突破训练上限。 |

### 💻 代码示例

```python
```python
import random
from collections import defaultdict
from typing import List, Tuple, Dict, Set

# ==================== 1. 图生成与任务定义 ====================

class GraphGenerator:
    """随机连通图生成器"""
    
    def __init__(self, num_nodes: int, seed: int = 42):
        self.num_nodes = num_nodes
        random.seed(seed)
    
    def generate(self) -> Tuple[List[int], List[Tuple[int, int]]]:
        """生成连通图: 返回 (节点列表, 边列表)"""
        nodes = list(range(self.num_nodes))
        edges = []
        
        # 确保连通: 先生成生成树
        parent = list(range(self.num_nodes))
        def find(x): 
            return x if parent[x] == x else find(parent[x])
        
        # 随机生成 n-1 条树边
        candidates = [(i, j) for i in range(num_nodes) for j in range(i+1, num_nodes)]
        random.shuffle(candidates)
        
        for u, v in candidates:
            if find(u) != find(v):
                parent[find(u)] = find(v)
                edges.append((u, v, 1))  # 权重固定为1
        
        # 添加随机额外边
        extra_edges = random.sample(candidates, k=len(candidates)//3)
        for u, v in extra_edges:
            if (u, v, 1) not in edges:
                edges.append((u, v, 1))
        
        return nodes, edges


def bfs_shortest_path(nodes: List, edges: List, start: int, end: int) -> List[int]:
    """计算最短路径 (BFS, 边权重=1)"""
    # 伪代码: BFS实现
    pass


# ==================== 2. 泛化维度操作化 ====================

class GeneralizationConfig:
    """泛化配置"""
    
    def __init__(self):
        self.train_nodes = 10   # 训练图规模
        self.test_nodes = 20    # 测试图规模 (空间迁移)
        self.train_max_len = 5  # 训练最大路径长度
        self.test_max_len = 10  # 测试最大路径长度 (长度扩展)


# ==================== 3. 训练数据生成 (因子化设计) ====================

class TrainingDataGenerator:
    """因子化设计生成训练数据"""
    
    def __init__(self, coverage_ratio: float = 0.5):
        self.coverage_ratio = coverage_ratio  # 三元组覆盖比例
    
    def generate_dataset(
        self, 
        num_graphs: int,
        max_path_len: int,
        coverage_ratio: float
    ) -> List[Dict]:
        """
        生成训练数据集
        返回格式: [(图描述, 查询, 正确答案路径), ...]
        """
        dataset = []
        
        # 覆盖所有 (起点,终点,路径长度) 三元组空间
        all_triples = self._enumerate_triples(max_path_len, coverage_ratio)
        
        for _ in range(num_graphs):
            # 生成随机连通图
            gen = GraphGenerator(num_nodes=10)
            nodes, edges = gen.generate()
            
            # 按覆盖比例采样三元组
            sampled = random.sample(all_triples, 
                                   k=int(len(all_triples) * coverage_ratio))
            
            for start, end, path_len in sampled:
                path = bfs_shortest_path(nodes, edges, start, end)
                if len(path) == path_len:  # 筛选正确长度
                    dataset.append({
                        "nodes": nodes,
                        "edges": edges,
                        "query": f"{start} -> {end}",
                        "answer": path
                    })
        
        return dataset
    
    def _enumerate_triples(self, max_len: int, ratio: float):
        """枚举所有可能的三元组 (起点,终点,长度)"""
        triples = []
        for length in range(3, max_len + 1):
            # 伪代码: 枚举所有节点对和路径长度组合
            pass
        return triples


# ==================== 4. 训练范式对比 ====================

# 4.1 监督微调 (SFT)
class SupervisedFineTuning:
    """SFT: 直接学习 问题→答案 映射"""
    
    def train(self, train_data: List[Dict], model):
        """伪代码: SFT训练循环"""
        for epoch in range(10):
            for sample in train_data:
                # 构建输入: "图描述 + 查询"
                prompt = self._format_prompt(sample)
                # 期望输出: 正确答案路径
                target = sample["answer"]
                
                # 计算损失并反向传播
                # loss = compute_loss(model(prompt), target)
                # loss.backward()
                pass


# 4.2 强化学习 (PPO/DPO)
class RLTraining:
    """RL: 基于验证器奖励优化"""
    
    def __init__(self):
        self.reward_model = PathValidator()
    
    def train(self, train_data: List[Dict], model):
        """伪代码: PPO/DPO训练循环"""
        for epoch in range(10):
            for sample in train_data:
                prompt = self._format_prompt(sample)
                
                # 生成多个候选响应
                responses = [model.generate(prompt) for _ in range(4)]
                
                # 计算奖励 (验证器检查)
                rewards = [self.reward_model.check(r, sample["answer"]) 
                          for r in responses]
                
                # PPO/DPO更新策略
                # policy_update(model, prompt, responses, rewards)
                pass


class PathValidator:
    """验证器: 直接检查输出路径是否正确"""
    
    def check(self, predicted_path: List[int], 
              ground_truth: List[int]) -> float:
        """返回奖励: 完全正确=1.0, 错误=0.0"""
        if predicted_path == ground_truth:
            return 1.0
        return 0.0


# ==================== 5. 推理策略: 推理时扩展 ====================

class InferenceWithTreeSearch:
    """树搜索 + 过程奖励模型"""
    
    def __init__(self, process_reward_model):
        self.prm = process_reward_model  # 过程奖励模型
    
    def inference(self, prompt: str, model, 
                  beam_width: int = 4, 
                  depth_limit: int = 20) -> str:
        """
        推理时计算扩展: 让模型"思考更久"
        伪代码: 树搜索+剪枝
        """
        candidates = []
        
        # 初始节点
        root = {"state": prompt, "score": 0.0, "path": []}
        candidates.append(root)
        
        for step in range(depth_limit):
            new_candidates = []
            
            for node in candidates:
                # 扩展子节点
                # children = model.expand(node["state"])
                children = []  # 伪代码: 展开下一层节点
                
                for child in children:
                    # 过程奖励打分
                    process_score = self.prm.evaluate(child)
                    node_score = node["score"] + process_score
                    
                    new_candidates.append({
                        "state": child,
                        "score": node_score,
                        "path": node["path"] + [child]
                    })
            
            # 剪枝: 保留top-k
            candidates = sorted(new_candidates, 
                              key=lambda x: x["score"],
                              reverse=True)[:beam_width]
            
            # 早停: 找到有效答案
            if self._is_valid_answer(candidates):
                break
        
        return self._select_best(candidates)


# ==================== 6. 实验主流程 ====================

def run_experiment():
    """关键实验: 图规模+路径长度+覆盖比例 梯度变化"""
    
    results = []
    
    # 梯度变化配置
    node_scales = [5, 10, 15, 20]
    length_scales = [3, 5, 7, 10]
    coverage_ratios = [0.1, 0.5, 1.0]
    
    for num_nodes in node_scales:
        for max_len in length_scales:
            for coverage in coverage_ratios:
                print(f"Config: nodes={num_nodes}, len={max_len}, cov={coverage}")
                
                # 生成数据
                data_gen = TrainingDataGenerator()
                train_data = data_gen.generate_dataset(
                    num_graphs=100,
                    max_path_len=max_len,
                    coverage_ratio=coverage
                )
                
                # 训练SFT模型
                sft = SupervisedFineTuning()
                # sft_model = train_sft(train_data)
                
                # 训练RL模型
                rl = RLTraining()
                # rl_model = train_rl(train_data)
                
                # 测试泛化能力
                test_graph = GraphGenerator(num_nodes=num_nodes + 5)  # 空间迁移
                test_nodes, test_edges = test_graph.generate()
```

### 🔢 核心公式

**公式 1**：

$$
```latex
\begin{align}
&\text{If a
$$

*含义*：If a model can solve smaller instances, can it compose them to solve larger-scale or struc-

**公式 2**：

$$
\begin{align}
\hat{f}_{\theta}(i,j\mid G)\ \text{successful} \iff \hat{f}_{\theta}(i,j\mid G)\in f(i,j\mid G).
\end{align}
$$

*含义*：, we deem ˆfθ(i, j | G) successful if it belongs to the set f(i, j | G).

---

## 🔬 实验结果

**数据集**：合成生成的数据集：
- 节点规模：5/8/10/15/20节点随机图
- 路径长度：1-12条边
- 覆盖比例：10%/25%/50%/75%/100%
- 数据量级：每配置数千条样本

**评价指标**：核心指标：
- **成功率(Success Rate, SR)**：输出路径与最优路径完全一致的比例
- **精确率/召回率**：路径边数正确但顺序可能有误的情况分析
- **长度分布**：模型成功率随目标路径长度的变化曲线

**主要结果**：

实验揭示了三个关键发现：

1. **空间迁移表现优异**：当训练数据覆盖达到50%+时，模型在完全陌生的地图上（节点数翻倍）仍能保持80%+成功率，说明模型学到了「局部贪心」或「动态规划」思路的可迁移表征。

2. **长度扩展系统性失败**：无论训练数据覆盖多高，当测试路径长度超过训练最大长度的1.5-2倍时，成功率急剧下降至接近零。这不是因为模型「没见过」这些节点，而是「无法递归组合」已有能力——这是典型的组合不稳定性。

3. **RL训练的作用被重新定义**：强化学习（PPO/DPO）显著加速了收敛、提高了训练稳定性，但并未显著扩展泛化边界。这提示RL更多是「更好地利用已有知识」，而非「获得新知识」。

4. **推理时计算有边际效益**：树搜索+PRM能带来10-20%的提升，但无法根本解决长度泛化问题，说明问题根植于模型本身。


**主要发现：**

- ✅ 模型展现出强空间迁移能力：可在全新地图上泛化，但长度扩展（递归组合）是系统性瓶颈
- ✅ 数据覆盖比例是能力上限的硬性约束：低于某阈值（~50%）时空间泛化也失效
- ✅ 强化学习提升训练稳定性但不拓展泛化边界——它让模型「用好已知」而非「学到未知」
- ✅ 推理时计算（如树搜索）带来边际提升，但无法根本解决递归不稳定性
- ✅ 最短路径问题的组合结构（递归最优子结构）恰好命中了LLM的泛化盲区


---

## 🎯 创新点分析

| 创新点 | 说明 |
|--------|------|
| 首个系统性解耦组合泛化因素的实验框架 | 通过最短路径这一可验证问题，首次将「数据覆盖」「训练范式」「推理策略」三个因素解耦独立研究，改变了以往「因素混杂、难以归因」的局面。 |
| 发现「递归不稳定性」是LLM组合泛化的核心障碍 | 首次实验证明：LLM可以「记住」不同长度的路径，但无法可靠地将「子问题最优解」组合成「整体最优解」——这直接挑战了「Transformer能自然实现组合泛化」的假设。 |
| 重新定义强化学习在LLM对齐中的作用 | 实验证明RL（如PPO）优化的是训练稳定性和样本效率，而非泛化能力边界——这对「通过更多RL就能让模型更聪明」的流行观点提出了有力质疑。 |

---

## 🏭 工业落地思考

**适用场景：**

- 🎯 智能路径规划与导航系统：模型需在新城市、新环境中生成最优路线
- 🎯 代码生成与程序综合：给定API组合约束下生成满足规范的代码
- 🎯 复杂任务规划（Agent系统）：多步骤推理中的子目标组合与验证


**实现难度**：中等

**工程挑战：**

- ⚠️ 真实场景的「长度扩展」更复杂：不是简单的边数增加，而是状态空间指数爆炸
- ⚠️ 验证成本高：最短路径有精确算法，而大多数实际任务的「最优解」难以获得
- ⚠️ 数据覆盖的工程挑战：如何在有限数据下最大化有效覆盖范围


**代码实现思路**：

核心实现思路：
1. 图生成器：使用Erdős–Rényi模型生成随机连通图
2. 验证器：BFS计算最短路径，批量验证模型输出
3. 实验框架：配置化管理训练/测试因子（节点数、路径长度、覆盖比例）
4. 推理时扩展：实现简单的Beam Search + 逐步剪枝
开源代码可参考论文GitHub仓库，核心模块可复用于其他组合优化问题（如旅行商、调度）的泛化研究。


---

## 📝 总结与展望

**核心收获**：LLM在最短路径任务中展现出空间迁移能力，但递归组合的不稳定性导致长度扩展失败——这一发现深刻揭示了「系统化泛化」的边界，也为设计更可靠的问题求解AI提供了关键指导。

**未来方向**：探索缓解递归不稳定性的方法（如结构化推理框架、记忆增强机制）、将实验框架拓展到更复杂的组合任务（如图着色、调度优化）、以及研究不同模型架构对组合泛化的影响。


---

## ❓ 常见问题

**Q：为什么选择最短路径而不是其他任务作为测试床？**

A：最短路径问题具有三个理想特性：①最优解可通过经典算法精确计算，实现零样本验证；②「长度」这一维度天然对应组合的深度，便于测试递归能力；③问题结构简单但组合复杂度高，能有效暴露LLM的泛化盲区而不被其他因素掩盖。


**Q：这篇论文对工业应用有什么直接指导意义？**

A：主要启示有三点：①如果你的应用涉及「组合泛化」（如路径规划、任务拆解），模型在训练数据覆盖范围外的表现可能远低于预期；②不要寄望于通过更多RL让模型「变聪明」，RL主要提升稳定性而非能力上限；③推理时计算（如搜索）能带来边际提升，但关键还是要在训练数据中覆盖足够的组合多样性。


**Q：「递归不稳定性」是什么意思？为什么LLM会在这里失败？**

A：递归不稳定性指：模型能独立解决子问题（如「A到B的最短路径」），但无法可靠地将多个子问题的解组合成整体最优解（如「A→B→C的完整最短路径」）。这可能源于Transformer的注意力机制在处理深层递归时的数值不稳定，或者训练数据中「成功组合」的正例太少，导致模型没有学到可靠的组合策略。


**Q：数据覆盖比例具体指什么？研究它有什么意义？**

A：覆盖比例指训练集中实际包含的(起点, 终点, 最短路径长度)三元组的比例。100%覆盖意味着所有可能的组合都见过；10%覆盖意味着只见过十分之一。这是一个可控的代理变量，用于量化「训练数据的多样性」如何影响泛化边界。实验发现覆盖比例低于50%时，连空间迁移都会失效——这量化了「需要多少训练数据才能泛化」。


---

## 📷 论文图片

**Figure 1**: The model successfully transfers to un-

![Figure 1](/home/sandbox/.openclaw/workspace/ai_daily/paper_cache/figure_3_0.png)

**Figure 2**: Spatial transfer success rate (SR)

![Figure 2](/home/sandbox/.openclaw/workspace/ai_daily/paper_cache/figure_7_1.png)

**Figure 3**: Interaction between coverage and

![Figure 3](/home/sandbox/.openclaw/workspace/ai_daily/paper_cache/figure_31_2.png)

---

*本文由 AI 推荐日报自动生成，仅供参考学习*
