# Prism：张量程序的符号化超优化

**通过符号图表示和两级搜索实现大规模张量程序的自动性能优化**


> 📅 预计阅读：约15分钟 | 
难度：高级 | 
arXiv: [2604.15272](http://arxiv.org/abs/2604.15272)


🏷️ 标签：`张量程序优化` | `符号执行` | `超级优化` | `深度学习编译器` | `LLM推理优化`


---

### 📌 TL;DR

- **一句话总结**：首个符号化超级优化器，通过符号图表示和两级搜索自动发现最优张量程序实现
- **核心贡献**：提出sGraph符号层次表示，压缩编码大规模张量程序空间，通过符号推理剪枝次优区域
- **实用价值**：在5个主流LLM工作负载上实现平均1.3倍加速，可直接用于深度学习编译器后端优化


---

## 📖 背景与动机

随着大语言模型(LLM)规模急剧增长，推理效率成为关键瓶颈。当前张量程序优化主要依赖手工调优或启发式搜索，前者耗时耗力且难以规模化，后者受限于搜索空间的指数爆炸。现有超级优化器虽能穷举小规模程序，但面对现代ML工作负载中的复杂张量运算时力不从心，无法处理并行维度、融合算子等真实场景中的变参问题。因此，亟需一种既具备穷举搜索的严谨性，又能适应大规模张量程序的自动优化方法。


**关键要点：**

- LLM推理性能优化是工程落地的关键挑战
- 传统优化方法在表达能力和搜索效率之间存在矛盾
- 符号化表示可压缩表示参数化程序空间
- 结合符号推理与自动调优是可行路径


---

## 💡 核心方法

### 方法概述

Prism提出符号化超优化框架，通过sGraph表示程序族并执行两级搜索：先构造符号图表示程序空间，再实例化为具体实现并自动调优。


### 详细设计

Prism的核心是sGraph（Symbolic Graph），一种层次化的符号表示方法。与传统具体图（μGraph）不同，sGraph使用符号变量表示维度、步长等执行参数，从而能够用单一节点表示一族程序变体。例如，一个MatMul操作可以用符号参数表示不同批量大小的情况。

第一级搜索是sGraph生成阶段。通过启发式探索结合符号剪枝，系统地构建符号图空间。关键在于利用运算符语义、代数恒等式和硬件约束进行符号级别的推理，快速排除必然次优的分支。例如，如果某个融合方案的寄存器需求超过硬件限制，直接剪枝。

第二级搜索是具体化与调优阶段。将符号图实例化为具体实现后，通过auto-tuning探索最优参数配置。Prism使用e-graph（等价图）数据结构支持高效的等价验证，能够快速判断两个程序是否语义等价。验证过程利用预定义的等价公理表（如分配律、结合律、维度重排等），通过重写规则系统完成证明。

并行维度是sGraph的重要特征。论文定义了并行化维度（parallelization dimension）和相应的并行操作符，允许多种合法并行策略共存于同一符号图中，通过后续调优确定最优方案。


### 🔧 关键组件

| 组件 | 说明 |
|------|------|
| sGraph（符号图） | 层次化符号表示，用符号变量编码程序族的执行参数，支持维度参数化、融合算子表示和并行策略编码 |
| 符号剪枝器 | 基于运算符语义、代数恒等式和硬件约束的符号推理引擎，在搜索过程中剪枝必然次优的符号图分支 |
| e-graph验证器 | 利用等价图数据结构和支持重写规则的验证系统，通过预定义公理表证明程序等价性 |
| Auto-tuner（自动调优器） | 实例化符号参数后，对具体配置空间进行调优，确定最优的线程块大小、分块参数等硬件相关参数 |

### 💻 代码示例

```python
```python
"""
Prism 方法简化实现示例
展示 sGraph、符号剪枝、等价验证和调优的核心思想
"""

from typing import List, Dict, Optional, Tuple, Any
from dataclasses import dataclass
from abc import ABC, abstractmethod

# ============ 1. 符号变量定义 ============

@dataclass
class SymbolicVar:
    """符号变量：表示维度、步长等执行参数"""
    name: str
    domain: Tuple[Any, Any]  # 取值范围
    constraints: List[str] = None  # 额外约束条件
    
    def __repr__(self):
        return f"sym({self.name}∈{self.domain})"


@dataclass
class Dim:
    """具体维度值"""
    value: int
    
    def __repr__(self):
        return str(self.value)


# ============ 2. sGraph 节点定义 ============

class SGraphNode(ABC):
    """符号图节点基类"""
    
    @abstractmethod
    def symbolic_prune(self, hw_constraints: Dict) -> bool:
        """符号剪枝：判断是否应该剪掉"""
        pass
    
    @abstractmethod
    def concretize(self, var_bindings: Dict[str, int]) -> 'CGraphNode':
        """具体化为具体图"""
        pass
    
    @abstractmethod
    def cost_estimate(self) -> SymbolicVar:
        """估计代价（符号形式）"""
        pass


class MatMulNode(SGraphNode):
    """矩阵乘法符号节点 - 代表一族不同批量大小的实现"""
    
    def __init__(self, batch: SymbolicVar, m: int, n: int, k: int):
        self.batch = batch
        self.m = m
        self.n = n
        self.k = k
        self.children: List[SGraphNode] = []
    
    def symbolic_prune(self, hw_constraints: Dict) -> bool:
        """符号级剪枝：检查寄存器需求是否超标"""
        # 符号级别推理：如果 batch * m * k > max_registers，则剪枝
        if self.batch.domain[1] * self.m * self.k > hw_constraints.get('max_registers', float('inf')):
            return True  # 剪枝掉必然次优的分支
        return False
    
    def concretize(self, var_bindings: Dict[str, int]) -> 'MatMulConcrete':
        """实例化具体批量大小"""
        concrete_batch = var_bindings.get(self.batch.name, self.batch.domain[0])
        return MatMulConcrete(concrete_batch, self.m, self.n, self.k)
    
    def cost_estimate(self) -> SymbolicVar:
        # 符号形式的 FLOPs 估计
        return SymbolicVar(
            name=f"flops_{self.batch.name}",
            domain=(self.batch.domain[0] * self.m * self.n * self.k,
                    self.batch.domain[1] * self.m * self.n * self.k)
        )
    
    def __repr__(self):
        return f"MatMul(sym_batch={self.batch})"


class ConvNode(SGraphNode):
    """卷积符号节点"""
    
    def __init__(self, cin: int, cout: int, kernel: int, h: SymbolicVar, w: SymbolicVar):
        self.cin = cin
        self.cout = cout
        self.kernel = kernel
        self.h = h
        self.w = w
    
    def symbolic_prune(self, hw_constraints: Dict) -> bool:
        # 符号剪枝逻辑
        return False
    
    def concretize(self, var_bindings: Dict) -> 'ConvConcrete':
        h_val = var_bindings.get(self.h.name, self.h.domain[0])
        w_val = var_bindings.get(self.w.name, self.w.domain[0])
        return ConvConcrete(self.cin, self.cout, self.kernel, h_val, w_val)
    
    def cost_estimate(self) -> SymbolicVar:
        return SymbolicVar(name="conv_cost", domain=(1, 1000))


# ============ 3. 具体计算节点 ============

@dataclass
class CGraphNode:
    """具体计算图节点"""
    op_type: str
    params: Dict
    parallel_dims: List['ParallelDim'] = None  # 并行化维度


@dataclass
class MatMulConcrete(CGraphNode):
    def __init__(self, batch: int, m: int, n: int, k: int):
        super().__init__(
            op_type="MatMul",
            params={'batch': batch, 'm': m, 'n': n, 'k': k}
        )


@dataclass
class ConvConcrete(CGraphNode):
    def __init__(self, cin: int, cout: int, kernel: int, h: int, w: int):
        super().__init__(
            op_type="Conv",
            params={'cin': cin, 'cout': cout, 'kernel': kernel, 'h': h, 'w': w}
        )


# ============ 4. 并行维度定义 ============

@dataclass
class ParallelDim:
    """并行化维度"""
    name: str
    max_degree: int
    cost_model: callable  # 评估该并行策略的代价
    
    def evaluate(self, graph: CGraphNode) -> float:
        return self.cost_model(graph)


# ============ 5. e-graph 等价验证 ============

class EGraph:
    """等价图：支持高效的等价验证"""
    
    def __init__(self):
        self.nodes: List[ENode] = []
        self.eclasses: Dict[int, List[int]] = {}  # e-class id -> node ids
    
    def add_rewrite_rule(self, pattern: str, replacement: str):
        """添加重写规则（如分配律、结合律等）"""
        self.rewrite_rules.append((pattern, replacement))
    
    def are_equivalent(self, expr1: SGraphNode, expr2: SGraphNode) -> bool:
        """使用 e-graph 判断两个符号表达式是否等价"""
        # 预定义的等价公理
        axioms = {
            'associative': [],      # 结合律
            'commutative': [],      # 交换律
            'distributive': [],     # 分配律
            'dim_reorder': [],      # 维度重排
        }
        
        # 通过重写规则系统进行证明
        def rewrite(expr: SGraphNode) -> SGraphNode:
            # 应用重写规则直到到达不动点
            return expr
        
        canon1 = rewrite(expr1)
        canon2 = rewrite(expr2)
        
        return self._normalize(canon1) == self._normalize(canon2)
    
    def _normalize(self, expr: SGraphNode) -> str:
        """标准化表达式"""
        return f"{type(expr).__name__}_{id(expr)}"


# ============ 6. 第一级搜索：sGraph 生成与符号剪枝 ============

class SGraphGenerator:
    """sGraph 生成器：启发式探索 + 符号剪枝"""
    
    def __init__(self, hw_constraints: Dict):
        self.hw_constraints = hw_constraints
        self.explored: List[SGraphNode] = []
    
    def generate_space(self, op_specs: List[Dict]) -> List[SGraphNode]:
        """
        系统地构建符号图空间
        返回所有未剪枝的候选符号图
        """
        candidates = []
        queue = [self._create_root(op_specs)]
        
        while queue:
            node = queue.pop(0)
            
            # === 符号剪枝 ===
            if node.symbolic_prune(self.hw_constraints):
                continue  # 剪枝必然次优的分支
            
            candidates.append(node)
            
            # 启发式探索：生成子节点
            for child in self._expand(node):
                queue.append(child)
        
        self.explored = candidates
        return candidates
    
    def _create_root(self, specs: List[Dict])
```

### 🔢 核心公式

**公式 1**：

$$
```latex
\begin{align}
m_{X,r,x^+} \tag{2}
\end{align}
```
$$

*含义*：–(2) (e.g., 𝑚𝑋,𝑟,𝑥+

**公式 2**：

$$
\begin{align}
m_{X,r,x} + m_{X,c,x} &\le 1,\\
&\quad \dots
\end{align}
$$

*含义*：mapping def.: 𝑚𝑋,𝑟,𝑥+𝑚𝑋,𝑐,𝑥≤1, ...

**公式 3**：

$$
```latex
\begin{align}
\text{dim match:}\quad & m_{X,c,x}=m_{W,r,x},\\
& m_{X,c,i}=m_{W,r,i
$$

*含义*：dim match: 𝑚𝑋,𝑐,𝑥=𝑚𝑊,𝑟,𝑥, 𝑚𝑋,𝑐,𝑖=𝑚𝑊,𝑟,𝑖

**公式 4**：

$$
\begin{align*}
-(2)) &: \text{each parallelogram}
\end{align*}
$$

*含义*：–(2)): each paral-

---

## 🔬 实验结果

**数据集**：5个主流LLM工作负载，包括GPT、LLaMA等模型的核心计算kernel

**评价指标**：执行时间、GPU内存占用、理论FLOP/s与实际性能比值

**主要结果**：

Prism在5个LLM工作负载上实现了平均1.3倍、最高达1.7倍的性能提升。实验覆盖了注意力机制、层归一化、矩阵乘法融合等关键算子。与现有超级优化器相比，Prism能处理更大规模的程序空间，搜索时间在可接受范围内（分钟级），而传统方法在同等规模问题上超时。符号剪枝策略有效减少了约60%的搜索空间，等价验证的e-graph方法相比朴素验证提速约5倍。


**主要发现：**

- ✅ 符号化表示显著压缩程序空间，支持高效穷举
- ✅ 符号剪枝能快速排除次优解，效果显著
- ✅ e-graph等价验证兼顾严谨性与效率
- ✅ Auto-tuning对最终性能有关键影响
- ✅ 融合策略的性能收益因硬件而异


---

## 🎯 创新点分析

| 创新点 | 说明 |
|--------|------|
| 首次将符号化思想引入张量超优化 | 提出sGraph表示方法，用符号变量编码程序族，避免对每个具体参数单独搜索，实现空间压缩 |
| 结合符号推理的启发式剪枝 | 利用运算符语义和硬件约束进行符号级别推理，提前剪枝必然次优的搜索分支，提升搜索效率 |

---

## 🏭 工业落地思考

**适用场景：**

- 🎯 LLM推理引擎的性能优化后端
- 🎯 深度学习编译器的自动调优模块
- 🎯 特定硬件（GPU/TPU）的算子库生成


**实现难度**：中等

**工程挑战：**

- ⚠️ 符号表示的完整性：需要覆盖所有合法的程序变体
- ⚠️ 等价公理的设计：既要完备又要避免冗余
- ⚠️ 符号剪枝的精确性：避免误剪导致遗漏最优解
- ⚠️ 大规模程序的scalability：符号图可能爆炸


**代码实现思路**：

核心实现分为三个模块：1)sGraph构建器，用符号变量替换具体维度参数；2)e-graph重写器，定义重写规则并实现模式匹配；3)参数实例化模块，结合profiling数据进行自动调优。可参考TVM的auto-scheduler设计理念进行实现。


---

## 📝 总结与展望

**核心收获**：符号化超优化为张量程序优化开辟了新范式，sGraph的两级搜索框架成功平衡了搜索严谨性与规模化需求

**未来方向**：扩展sGraph支持的算子类型、提升符号剪枝的自动化程度、探索多硬件后端的统一优化框架


---

## ❓ 常见问题

**Q：sGraph与TVM的Tensor Expression有何区别？**

A：TVM的TE是具体表示，需要枚举所有参数组合；sGraph使用符号变量编码程序族，一个节点可表示多个具体程序变体，从而压缩搜索空间。


**Q：符号剪枝会漏掉最优解吗？**

A：不会。符号剪枝仅在能数学证明某个分支必然次优时才会剪枝，如寄存器溢出、维度不匹配等约束违反，确保完备性。


**Q：Prism的搜索时间是否适合在线使用？**

A：论文侧重于离线优化场景，对于固定的LLM kernel进行一次搜索后可反复使用。对于动态shape场景需要进一步优化。


---

## 📷 论文图片

---

*本文由 AI 推荐日报自动生成，仅供参考学习*
